name: Data Tests
run-name: 'Data Tests run by @${{ github.actor }}, event: ${{ github.event_name }}'
on:
  workflow_call:
  workflow_dispatch:
concurrency:
  group: data-test ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  job-not-implemented-yet:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Step not implemented yet
        run: |
          echo "This job is not yet implemented"
          exit 1
